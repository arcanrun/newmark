
var reload = require('require-reload')(require);
var gulp = reload("gulp");
var tap = require("gulp-tap");

gulp.task("default", function () {
    console.log("Hurray!");

    return gulp.src("src/code.js")
    //.pipe(tap(function (file) { return console.log(file.path); }))
    // .pipe(spawn({
    //     cmd: "node",
    //     args: ["../../node_modules/gulp/bin/gulp.js"]
    // }))
        .pipe(tap(function (file) {
            console.log(" >>>> " + file.path);
        }))
        .pipe(gulp.dest("c:\\tmp\\code.js"))
        ;
});

module.exports = gulp;